Tu es expert en prompts pour agents de code, architecte front senior et expert de l'usage de Copilot en équipe.

Contexte : nouveau projet front React + TypeScript en entreprise, dossier vide. Nous avons des user stories et des maquettes Figma, exportées en images dans docs/inputs/maquettes/ (pas d'accès au MCP Figma). Le back est développé en parallèle et ne sera pas disponible au début. Les formats de données et la liste complète des profils ne sont pas encore connus : ils arriveront au fur et à mesure. Les maquettes montrent au moins deux vues : pilote et responsable pilote. Il y aura des appels API et des événements poussés par le back en SSE. Authentification en SSO. Gestion de profils : vues et droits différents selon le profil. Tout doit être simulé au départ (données, API, SSE, authentification et profils), puis basculé vers le réel au fur et à mesure de l'arrivée des API, sans réécriture. Le dépôt est sur GitLab (GitLab CI, merge requests). Copilot est utilisé uniquement en local dans VS Code : pas d'agent Copilot sur le dépôt, pas de revue automatique sur les merge requests, pas de serveur MCP. Notre quota d'IA est limité : rien d'automatique, tout est déclenché manuellement par un développeur, et chaque requête doit être aussi efficace que possible. Le processus cible : pour chaque feature à développer, un développeur lance Copilot avec les fichiers d'instructions du dépôt, et Copilot produit la feature conforme aux conventions en un minimum d'échanges. Nous sommes deux développeurs React et nous tranchons ensemble.

Objectif : cadrer avant d'écrire du code : architecture, normes de développement, stratégie de simulation, gestion des profils et des droits, intégration de Copilot dans le projet. Le tout défendable devant des chefs de projet et des architectes, et maintenable sur la durée.

Livrable : écris dans docs/00-kickoff.md le prompt complet que je te donnerai ensuite pour réaliser ce cadrage. Ce prompt doit :

1. Procéder par étapes, avec arrêt obligatoire entre chaque ; jamais d'étape suivante sans notre accord.
2. Tout écrire dans des fichiers, et tenir un fichier d'état pour reprendre dans un nouveau chat, incluant les hypothèses en attente de validation. S'appuyer sur ce fichier d'état plutôt que relire le dépôt à chaque étape.
3. À chaque étape, avant de produire, poser ses questions dès qu'une information manque ou qu'une hypothèse est nécessaire, et attendre nos réponses. Regrouper les questions d'une étape en un seul message.
4. Installer dès le début des instructions Copilot de phase de cadrage, courtes.
5. Nous faire déposer ce dont nous disposons avant toute analyse : user stories, maquettes exportées avec leur README (écrans, profils, navigation), contraintes. Travailler avec ce qui est connu ; pour ce qui manque (formats de données, profils et droits), formuler des hypothèses explicites dérivées des maquettes et des user stories, les marquer « à valider », les consigner dans le fichier d'état, et prévoir comment elles seront remplacées par les vrais éléments quand ils arriveront.
6. Pour chaque décision : au moins 2 à 3 options, avantages et inconvénients de chacune, sa recommandation et les raisons précises de cette recommandation dans notre contexte, le mécanisme d'application (lint, TypeScript, GitLab CI, revue de merge request). Présenter chaque décision de façon à alimenter notre discussion à deux, avec une colonne « notre choix » vide à remplir. Jamais de décision à notre place ; une décision n'est prise que lorsque nous l'avons écrite.
7. Quand le contrat de données existe, le traiter comme seule source de vérité. Tant qu'il n'existe pas, dériver des maquettes un contrat provisoire (données affichées et saisies par écran), clairement marqué provisoire, à proposer au back ; aucun format inventé au-delà de ce que les maquettes et les user stories justifient.
8. Traiter la simulation (données, API, SSE, SSO, profils) comme une couche à part entière, réutilisée par les tests, avec connexion simulée sous n'importe quel profil, bascule simulé/réel par configuration et par fonctionnalité, et ajout d'un profil ou d'un modèle de données à faible coût.
9. Définir les droits par profil en un seul endroit, sous forme de données et non de code dispersé, en commençant par les profils visibles dans les maquettes (pilote, responsable pilote), testés, sans que l'affichage conditionnel soit jamais la seule barrière de sécurité.
10. Interdire tout code avant validation des normes.
11. Pour chaque bibliothèque ou pattern recommandé, vérifier l'état actuel sur la documentation officielle, noter source et date, signaler ce qui n'a pas pu être vérifié.
12. Inclure une étape d'objections où il attaque ses propres recommandations en tant qu'architecte sceptique et chef de projet.
13. Concevoir les fichiers Copilot pour l'économie de contexte : instructions générales très courtes (stack, commandes, structure, règles bloquantes, fichiers exemples à imiter) ; règles détaillées uniquement dans des instructions par chemin, chargées seulement pour les fichiers concernés ; chaque règle sous forme de pointeur vers un fichier exemple du dépôt plutôt que de longue description ; une feature de référence réelle servant de modèle ; interdire l'exploration inutile du dépôt.
14. Se terminer par la création des fichiers Copilot du projet, limités aux briques manuelles qui fonctionnent en local dans VS Code, dans le dossier .github/ (lu par l'extension VS Code même sur GitLab) : instructions générales (.github/copilot-instructions.md), instructions par chemin (.github/instructions/*.instructions.md avec applyTo), prompts réutilisables (.github/prompts/*.prompt.md, dont un prompt « nouvelle feature » qui charge uniquement la spec de la feature, le contrat concerné et la feature de référence, et un prompt « revue selon nos conventions » lancé à la main avant chaque merge request). Aucun hook, aucun déclenchement automatique. Vérifier sur la documentation officielle Copilot (page « customization cheat sheet ») et dans notre environnement que ces briques sont disponibles avant de les utiliser. Créer un template de merge request GitLab (.gitlab/merge_request_templates/) avec la checklist des normes. Rédiger un guide d'usage de l'IA pour l'équipe (docs/ai-playbook.md : le processus par feature du début à la merge request, quel modèle pour quel type de tâche, quand utiliser Ask, Plan ou Agent, un chat par feature, comment préparer une requête pour qu'elle aboutisse en un échange, comment relire du code produit par l'IA, comment transformer une remarque de revue en instruction). Puis un test à blanc sur une user story, en notant le nombre d'échanges nécessaires et le contexte chargé inutilement, pour ajuster les instructions.

Pose-moi d'abord tes questions, puis rédige le fichier, puis relis-le en critique et corrige-le. Français, dense.